/// <reference types="cypress" />

// challenge #1: create a new board using API in the beforeEach and add an alias to it
beforeEach( function() {

}) 

// challenge #2: open the board by using the alias you created in beforeEach hook
it('opens a board', function() {

})

// challenge #3: create a new list inside the created board
it('creates a new list', function() {

})

// challenge #4: create a new list and a new card that will be inside the board
it('creates a new list and a new card', function() {

})

// 💯 extra credit challenge: create a new list, card, open that board and assert that the
// card detail contains the list name
it('shows list name in the card detail', function() {

})